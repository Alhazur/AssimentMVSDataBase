@model Student

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>
<h4>Student's info</h4>
<hr />

<table class="table table-striped table-dark">
    <thead>
        <tr>
            <th class="col-2">Name</th>
            <th class="col-2">Phone</th>
            <th class="col-2">Location</th>
            <th class="col-2">Teacher</th>
            <th class="col-3">Students</th>
            <th class="col-3">Options</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>@Html.DisplayFor(model => model.Name)</td>
            <td>@Html.DisplayFor(model => model.Phone)</td>
            <td>@Html.DisplayFor(model => model.Location)</td>
            <td>??</td>
            <td>??</td>

            <td>
                <a class="btn btn-primary" asp-controller="Student" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>
                <a class="btn btn-danger" asp-action="Index">Back to List</a>
            </td>

        </tr>
    </tbody>
</table>
@*<div>
        @if (Model.StudentsCourses != null)
        {
            @foreach (var item in Model.StudentsCourses)
            {
                @Html.DisplayFor(modelItem => item.CourseId)
            }
        }
    </div>*@